# Somente desenvolvimento: banco separado do MySQL e SMS simulado no terminal.
server.address=127.0.0.1
spring.datasource.url=jdbc:h2:file:./data/capricha-local;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false
app.sms.mode=console
# Use EMAIL_MODE=smtp no arquivo local de segredos para enviar de verdade.
app.email.mode=${EMAIL_MODE:console}
management.health.mail.enabled=false
